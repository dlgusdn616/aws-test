<% include partials/header.ejs %>
        <% if (articles.length > 0) {%>
                    <% articles.forEach((article, index) => { %>
                        <% if (index != 0 && index % 3 == 0) { %>
                            <div class="row">
                        <% } %>
                        <div class="col-md-4">
                            <div class="card mb-4 shadow-sm">
                                <img class="card-img-top" src=<%= previews[index].image %> data-src="holder.js/100px225?theme=thumb&bg=55595c&fg=eceeef&text=Thumbnail" alt="Card image cap" style="height: 225px; width: 100%;">
                                <div class="card-body">
                                <p class="card-text"><%= previews[index].description %></p>
                                <div class="d-flex justify-content-between align-items-center">
                                    <div class="btn-group">
                                        <a href=<%= article.article %> class="btn btn-sm btn-outline-secondary">View</a>
                                        <a href="/edit/<%= article.id %>" class="btn btn-sm btn-outline-secondary">Edit</a>
                                    </div>
                                    <small class="text-muted">9 mins</small>
                                </div>
                                </div>
                            </div>
                        </div>
                        <% if (index != 0 && index % 3 == 0) { %>
                            </div>
                        <% } %>
                    <% }) %>
        <% } else { %>
            <!-- when aritcle.length == 0 -->
        <% } %>
                <!-- container -->
                </div>
            <!-- album -->
            </div>
<% include partials/footer.ejs %>